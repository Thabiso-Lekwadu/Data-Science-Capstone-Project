# Streamlit-container requirements. Rename to requirements.docker.txt in the app
# repo root (or point the Dockerfile's COPY/RUN lines at this filename
# instead) if you don't already have your own requirements.docker.txt / uv.lock.
#
# IMPORTANT: xgboost / lightgbm / catboost / scikit-learn versions here
# should match requirements.docker.txt (or your pipeline repo's own
# requirements) as closely as possible — this is what lets the .pkl models
# the pipeline container trains actually unpickle and predict correctly here.

streamlit>=1.36,<2

pandas>=2.1,<3
numpy>=1.26,<3
scikit-learn>=1.4,<2
xgboost>=2.0,<3
lightgbm>=4.1,<5
catboost>=1.2,<2

plotly>=5.20,<6
shap>=0.45,<1
matplotlib>=3.8,<4

openpyxl>=3.1,<4